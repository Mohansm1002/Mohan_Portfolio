spring.datasource.url=jdbc:mysql://localhost:3306/portfolio1?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=password
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
# Enable CORS for all origins (to be refined later)
# This is a simple global CORS config via properties (Spring Boot 2.4+ supports it)
# However, we will also configure a bean in code.
